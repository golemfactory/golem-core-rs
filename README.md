PoC for actor model networking in Rust

### Running

In terminal, type:
```
$ cd net
$ cargo run 127.0.0.1:10001
``` 

Start a second instance with:
```
$ cargo run 127.0.0.1:10002 127.0.0.1:10001
```
